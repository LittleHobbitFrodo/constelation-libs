[package]
name = "libpages"
description = "Library used to manage virtual address spaces"
version = "0.1.0"
edition = "2024"
licence = "MIT"

[dependencies]

libmem = { path="../libmem", version = "0.1.0" }
liblock = { path="../liblock", version = "0.1.0" }

[dev-dependencies]
libtest = { path="../libtest", version = "0.1.0" }

#   To test libpages against
#x86_64 = { version = "0.15.5", optional = true, default-features = false }
#spin = { path = "../spin-rs", version = "0.11.0" }


[features]
raw=[]
testing=[] #["dep:x86_64"]
bootloader=[]
