[package]
name = "librand"
description = "Provides various random number generators"
version = "0.1.0"
edition = "2024"


[features]
testing=["sextant"]
sextant=[]  #   uses sextant's hook randomization hook

[dependencies]
libtest = { path="../libtest", features = ["testing", "harness"] }
